---
// DEPRECATED: esta ruta ya no se usa. El panel real es
// /gestion-roster-x9f2 (ver AGENT.md, sesion "Sistema de auth del panel").
// Este archivo se dejo vacio (redirect) en vez de borrado porque el MCP de
// filesystem no expone delete; borrar admin.astro a mano cuando se pueda.
export const prerender = false;
return Astro.redirect("/gestion-roster-x9f2");
---
