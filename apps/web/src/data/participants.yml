# Datos de ejemplo (demo). Reemplazar con la lista real de participantes.
# Si este archivo queda vacio o con formato invalido, el sitio usa un fallback mock.
#
# `stats` es una lista libre de { label, value } (0-100). Cada participante
# puede tener labels distintas y en cualquier cantidad; se renderiza tal
# cual venga, no hay campos fijos.
