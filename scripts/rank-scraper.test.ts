// DEPRECATED: reemplazado por test-rank-scraper.test.ts (Bun exige ".test."
// o ".spec." en el nombre del archivo para reconocerlo como test — "bun run
// test:scrapping" apuntaba aca y fallaba con "did not match any test
// files"). Este archivo se dejo vacio en vez de borrado porque el MCP de
// filesystem no expone delete; borrarlo a mano cuando se pueda.
export {};
