{
  "name": "velada-lol-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter web dev",
    "build": "bun run --filter web build",
    "setup:db": "bun run scripts/setup-supabase.ts",
    "setup:cf-secrets": "bun run scripts/setup-cloudflare-secrets.ts",
    "backup:db": "bun run scripts/backup-db.ts",
    "resend-invite": "bun run scripts/resend-invite.ts",
    "test:scrapping": "bun test scripts/test-mmradar-scraper.test.ts",
    "test:roulette": "bun test scripts/test-roulette.test.ts",
    "calibrate:rank": "bun test scripts/test-rank-calibration.test.ts"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "zod": "^3.22.4"
  }
}
